---
name: "PR Validate"

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize

jobs:
  Validate:
    uses: gucio1200/gh-workflows/.github/workflows/general_pr-title.yml@v7.14.6
    secrets: inherit
